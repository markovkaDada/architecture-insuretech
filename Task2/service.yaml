apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-service
spec:
  type: NodePort
  selector:
    app: scaletestapp  # Селектор для поиска подов
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080